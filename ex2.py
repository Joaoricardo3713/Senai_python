# base x altura / 2 formula do triangulo resolva 
base = float(input("digite o valor da sua devida base"))
altura = float(input("digite o valor da sua devida altura"))
resultado = float ((base*altura)/2)
print(f" a area do triangulo de base {base} e altura {altura} é {resultado}")
